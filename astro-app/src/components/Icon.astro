---
import type {Icon} from "../utils/sanity"
import { Image } from 'astro:assets';

interface Props {
  icon: Icon
};

const { icon } = Astro.props;
const imgSrc = `data:image/svg+xml,${icon.svg}`;
---
<div>
  <Image src={imgSrc} alt={icon.name} inferSize={true} />
</div>
